# Environment Configuration Example
# Copy this file to .env and fill in your actual values

# ==================== FIREBASE ====================
# Firebase configuration (already set in lib/firebase_options.dart)
# To add iOS/Android support, run: flutterfire configure

# ==================== CLOUDFLARE WORKER ====================
# Cloudflare Worker URL (for email notifications)
# Deploy the worker from cloudflare-worker/ directory first
CLOUDFLARE_WORKER_URL=https://your-worker.your-subdomain.workers.dev

# Resend API Key (set as environment variable in Cloudflare Worker dashboard)
# Get your key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# ==================== EMAIL CONFIGURATION ====================
# Default recipient email for all order notifications
DEFAULT_EMAIL=your-email@example.com

# ==================== MERCHANT/BRANCH IDs ====================
# For local development, you can hardcode merchant/branch IDs
# In production, these come from URL params or slug routing
# Example: ?m=merchant123&b=branch456 or /s/your-slug
MERCHANT_ID=
BRANCH_ID=

# ==================== OPTIONAL ====================
# Firebase Emulator (for local development)
# Enable with: flutter run --dart-define=USE_EMULATORS=true
USE_EMULATORS=false
